version: '3.8'

services:
  simple-resume-demo:
    build:
      context: ./src
      dockerfile: Dockerfile
    container_name: simple_resume_demo
    volumes:
      - ./src:/app
    environment:
      - PYTHONUNBUFFERED=1
